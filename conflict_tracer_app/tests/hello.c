#include <stdio.h>
#include <fcntl.h>
#include <sys/types.h>
#include <unistd.h>
#include <string.h>
#include <errno.h>
#include <wait.h>

int main(void) {
    // printf("Creating file: bar.txt\n");
    // int fd = open("bar.txt", O_CREAT | O_WRONLY | O_TRUNC, S_IRWXU);

    // if (fork() == 0) {
    //     int ret = unlink("foo");
    // }
    
    // int ret2 = unlink("foo");
    
    // wait(NULL);
    printf("Hi");
    return 0;
}   